#pragma once

#include "../../core_hermes/hermes.h"
#include "../pillar_ids.h"

// Pillar p07 — GHOST-G (stealth + minimal runtimes).

hermes_status_t p07_handle(const hermes_msg_t* msg, hermes_msg_t* out_response);
