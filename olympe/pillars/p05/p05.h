#pragma once

#include "../../core_hermes/hermes.h"
#include "../pillar_ids.h"

// Pillar p05 — MORPHOS-G (UI + graphics).

hermes_status_t p05_handle(const hermes_msg_t* msg, hermes_msg_t* out_response);
